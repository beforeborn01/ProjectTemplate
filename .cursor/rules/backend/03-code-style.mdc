---
globs: *.java
alwaysApply: true
description: Java 代码风格规范，包括格式、命名、注释、异常处理和日志规范
---
# Java 代码风格规范

## 格式规范
1. 使用 4 空格缩进，不使用 tab
2. 行宽不超过 120 字符
3. 方法间空一行
4. 类的成员变量放在类的顶部
5. import 语句按字母顺序排序

## 命名规范
1. 类名使用 PascalCase 风格 (如: `DemoController`)
2. 方法名和变量名使用 camelCase 风格 (如: `getUserInfo`)
3. 常量使用全大写+下划线 (如: `MAX_COUNT`)
4. 包名全部小写 (如: `com.bc.template.web`)

## 注释规范
1. 类必须有 Javadoc 注释，描述类的用途
2. 公共方法必须有 Javadoc 注释，描述功能、参数和返回值
3. 复杂逻辑需要添加行内注释

## 异常处理
1. 不允许捕获异常后不处理或打印日志后吞掉异常
2. 推荐使用自定义异常类 `ProjectTemplateException`
3. 在 Web 层通过全局异常处理器 `GlobalRequestExceptionHandler` 处理异常

## 日志规范
1. 使用 SLF4J 作为日志门面
2. 日志对象命名为 `log`
3. 使用不同日志级别: ERROR, WARN, INFO, DEBUG

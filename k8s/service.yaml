apiVersion: v1
kind: Service
metadata:
  name: template
  labels:
    app: template
spec:
  type: ClusterIP
  selector:
    app: template
  ports:
    - name: http
      port: 9915
      targetPort: 9915
      protocol: TCP
